export enum Types {
    Data = `Data`,
    User = `User`,

    Grid = `Grid`,
    Board = `Board`,
    List = `List`,
    Item = `Item`,
    Task = `Task`,

    Feature = `Feature`,
    Notification = `Notification`,
}

export enum GridTypes {
    Personal = `Personal`,
    Work = `Work`,
    Shared = `Shared`,
    Public = `Public`,
    Private = `Private`,
    Archived = `Archived`,
}